@page "/"

@inject NavigationManager navManager;

<h1>Hello, world!</h1>

<ul>
    <li>
        <a href="/chat">View Chat</a>
    </li>
    <li>
        <a href="/showcase">View Extensions Showcase</a>
    </li>
</ul>

@code {
    BlazorOBSUserSettings userSettings = BlazorOBSUserSettings.Get();

    protected override void OnInitialized()
    {
        if (userSettings == null)
        {
            navManager.NavigateTo("/login");
        }

        // TODO: verify the latest token on load

        base.OnInitialized();
    }

}